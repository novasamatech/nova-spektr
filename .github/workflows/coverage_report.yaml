name: Coverage report

on:
  pull_request:
    # types: [ labeled ]

jobs:
  test-coverage:
    # if: ${{ github.event.label.name == 'coverage-check' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: ⚙️ Install dependencies
        uses: ./.github/workflows/install-pnpm

      - name: 🧪 Run tests with coverage
        run: pnpm test:coverage

      - name: Jest coverage comment
        id: coverage-report
        uses: MishaKav/jest-coverage-comment@main
        with:
          coverage-summary-path: ./coverage/coverage-summary.json
          title: Current coverage report
          summary-title: Summary
          badge-title: Covered
          hide-comment: false
          create-new-comment: false
          hide-summary: false
          junitxml-title: Detailed analysis
          junitxml-path: ./junit.xml
          coverage-title: Coverage by files
          coverage-path: ./coverage.txt

      - name: Is the threshold satisfied?
        if: ${{ steps.coverage-report.outputs.coverage < 50 }}
        run: exit 1

