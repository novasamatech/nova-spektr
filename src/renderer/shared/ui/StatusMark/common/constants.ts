import { Variant } from './types';

export const DotStyle: Record<Variant, string> = {
  waiting: 'bg-icon-primary-default',
  success: 'bg-icon-positive-default',
  warn: 'bg-icon-warning-default',
  error: 'bg-icon-negative-default',
};
