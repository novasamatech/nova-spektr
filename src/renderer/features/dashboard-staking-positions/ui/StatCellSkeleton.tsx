import { Skeleton } from '@/shared/ui-kit';

/** The shimmer a drawer stat cell shows while its value is still on its way. */
export const StatCellSkeleton = () => <Skeleton width="72px" height="14px" testId="StatCellSkeleton" />;
