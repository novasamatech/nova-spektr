/**
 * Balance test fixtures
 *
 * Pre-configured balance data for testing including:
 *
 * - Standard balances (various amounts)
 * - Low balances (for testing insufficient funds)
 * - Asset balances (USDT, etc.)
 * - Helper functions for creating custom balances
 */

export * from './balances';
