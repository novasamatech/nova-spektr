import { TEST_IDS } from '@/shared/constants';

export class AssetsSettingsModalElements {
  hideZeroBalances = 'Hide zero balances';
  pageView = TEST_IDS.ASSETS.PAGE_VIEW;
  chainCentric = 'Chain-centric';
  tokenCentric = 'Token-centric';
}
