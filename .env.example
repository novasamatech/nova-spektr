# Copy to `.env` (git-ignored). Values here are read by the renderer build; never commit real secrets.

# WalletConnect Cloud project id. Create one at https://cloud.reown.com (formerly WalletConnect Cloud).
# Leave empty to build without WalletConnect / Nova Wallet pairing (the feature is hidden).
WALLET_CONNECT_PROJECT_ID=
