# Copy to `.env` (git-ignored). Values here are read by the renderer build.

# WalletConnect Cloud project id. Create one at https://cloud.reown.com (formerly WalletConnect Cloud).
# Empty is allowed in development (WalletConnect / Nova Wallet pairing is hidden); staging/production builds fail without it.
WALLET_CONNECT_PROJECT_ID=
